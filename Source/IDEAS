- use circle selectors for each r,g,b,a ? remove kb shortcuts
- vertex painting ...

- why are we NOT using PARTS/WINDINGS for UI ?? lol...
- edit the UI as a model ? ... using procedures for drawing text etc...hm.

